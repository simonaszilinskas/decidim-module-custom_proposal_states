<!-- replace "erb[loud]:contains('f.collection_radio_buttons :internal_state')" -->
        <%= f.collection_radio_buttons :internal_state, available_states, :token, ->(a) { translated_attribute(a.title) }, prompt: true %>
